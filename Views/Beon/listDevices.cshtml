@model IEnumerable<String>

@{
    ViewData["Title"] = "listDevices";
    Layout = "~/Views/Shared/_LayoutBeonPrincipal.cshtml";
}
<div class="border p-3">
    <h3 class="text-left  ">Tracker List</h3>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Device Id</th>
        </tr>
    </thead>
    <tbody>
       
      
        @foreach (String dev in Model)
        {
        <tr>
            <th scope="row"> <a asp-controller="Beon" asp-action="TestDashboard" asp-route-IdDevice="@dev"> @dev</a></th>
        </tr>
        }
    </tbody>
</table>
